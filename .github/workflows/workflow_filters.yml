name: testing event activities and filters
on:
  workflow_dispatch: 
  push:
    branches:
      - main
      - '!feauture/*'
  pull_request:
    types:
      - opened
      - closed
    branches:
      - main
    paths-ignore:
      - README.md
jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: testing events
        run: |
          echo "testing event activities and filters"
          echo "skipping actions tested"
      
